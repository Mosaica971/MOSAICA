\chapter{Porter et rendre traçable}
\label{ch:porter}
% BUDGET : 6 pages (cf. memoire/PLAN.md, section 4).
% THESE DU CHAPITRE : la fidelite a l'ARTEFACT -- et non a une realite supposee -- est ce qui
% rend opposable tout desaccord ulterieur entre les deux modeles.
%
% NOTE : les blocs mathematiques sont CONSERVES. Ce ne sont pas de la prose a reecrire, ce
% sont les objets formels du memoire, et \eqref{eq:objectif}, \eqref{eq:mo} et
% \eqref{eq:indicateur} sont appeles depuis les chapitres 3 et 4.

\section{Formulation du programme}
\label{sec:formulation}

Soit $P$ l'ensemble des parcelles, $C$ l'ensemble des cultures, et $E \subseteq P \times C$
l'ensemble des couples \emph{éligibles} (§~\ref{sec:architecture}). La variable de décision est
\[
  Y_{p,c} \in \{0,1\}, \qquad (p,c) \in E,
\]
qui vaut 1 si la parcelle $p$ porte la culture $c$. L'objectif est la marge brute agrégée,
corrigée du risque :
\begin{equation}
  \max \; \sum_{(p,c) \in E} Y_{p,c} \; s_p \; m_c \; \bigl(1 - a_{f(p)} \, v_c \bigr),
  \label{eq:objectif}
\end{equation}
où $s_p$ est la surface de la parcelle (ha), $m_c$ la marge brute unitaire (\eur/ha), $f(p)$
l'exploitation de la parcelle, $a_f$ son coefficient d'aversion au risque et $v_c$ un
coefficient de perte propre à la culture. À chaque parcelle, au plus une culture :
\begin{equation}
  \sum_{c \,:\, (p,c) \in E} Y_{p,c} \;\le\; 1, \qquad \forall p \in P.
  \label{eq:unicite}
\end{equation}
Par exploitation, un plafond de main d'\oe uvre,
\begin{equation}
  \sum_{p \in P_f} \sum_{c} Y_{p,c} \, s_p \, h_c \;\le\; \sigma \, H_f,
  \label{eq:mo}
\end{equation}
où $h_c$ est le besoin en travail (h/ha), $H_f$ la main d'\oe uvre observée et $\sigma$ un
facteur de desserrement. Enfin, des bornes portant sur un \emph{indicateur} quelconque,
\begin{equation}
  \sum_{(p,c) \in E} Y_{p,c} \, s_p \, w_p \, r_c \;\lesseqgtr\; T,
  \label{eq:indicateur}
\end{equation}
où $r_c$ est un taux par hectare (kg d'azote, \gls{ift}, heures, euros de subvention) et $w_p$
un poids de parcelle.

Le programme est donc un \gls{milp} purement binaire : il n'y a aucune variable continue, et
l'unicité~\eqref{eq:unicite} est une inégalité — une parcelle peut rester sans culture. L'ensemble $E$ rend le problème traitable computationnellement. Le produit $P \times C$
compte environ deux millions de couples. Restreindre cet ensemble aux couples agronomiquement
éligibles réduit le compte à \num{\mesBinaires} (tableau~\ref{tab:programme}). C'est une décision de modélisation qui intervient avant l'optimisation : les couples écartés par le masque
d'éligibilité ne pourront pas réapparaître grâce à une contrainte. Le
chapitre~\ref{ch:calibrer} montrera que le masque impose à lui seul un plancher d'écart à l'observé.

On utilise une fonction objective, c'est-à-dire une quantité que l'on cherche à optimiser. On choisit la fonction de Markowitz que l'on cherchera ici à maximiser.
--- INSERE ICI LA FONCTION COUT ET DEFINI ET EXPLIQUE BRIEVEMENT CE QUI LA COMPOSE ---

Deux précisions concernant la fonction objective :
- La pénalité est linéaire en surface, et aucune covariance entre cultures n'intervient. Le programme appartient donc à la classe
\gls{milp} plutôt que quadratique. Il est donc résoluble par \gls{highs}. 
- Le revenu net que produit le rapport de run n'est pas la quantité maximisée.
Il intègre également le coût de la main d'oeuvre, alors qu'elle n'est \textbf{pas} monétisée dans les coûts 
variables hérités du \gls{gams} : elle n'apparaît que comme la ressource plafonnée de~\eqref{eq:mo}. 

L'annexe~\ref{ann:formulation} donne l'inventaire complet des
ensembles, paramètres et contraintes, avec pour chacun le nom qu'il porte dans le \gls{gams}
d'origine.

\begin{table}[tbp]
  \centering
  \caption{Taille du programme et coût de sa résolution. Les durées sont mesurées sur le
  territoire complet ; l'étendue \emph{min--max} porte sur vingt résolutions du \emph{même}
  problème, à taille constante.}
  \label{tab:programme}
  \small
  \begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}Xr@{}}
    \toprule
    Couples parcelle $\times$ culture potentiels & $\sim$\num{2000000} \\
    Variables binaires déclarées (calibration retenue) & \num{\mesBinaires} \\
    Variables binaires déclarées (configuration prospective) & \num{\mesBinairesRef} \\
    \dots{} si les \num{25} variantes symétriques sont rouvertes (§~\ref{sec:tractabilite})
      & \num{\mesBinairesKarus} \\
    \midrule
    Résolution la plus rapide des vingt enregistrées & \SI{\mesSolveMin}{\second} \\
    Résolution la plus lente des vingt enregistrées & \SI{\mesSolveMax}{\second} \\
    Coefficient de variation des durées & \SI{\mesSolveCV}{\percent} \\
    Même problème, amorçage à froid puis à chaud
      & \SI{\mesWarmAvant}{\second} $\rightarrow$ \SI{\mesWarmApres}{\second} \\
    Écart d'optimalité relatif imposé au solveur & \SI{\mesGapMip}{\percent} \\
    \bottomrule
  \end{tabularx}
\end{table}

\section{Protocole}
\label{sec:parite}

Le protocole du portage prévoit une reproduction fidèle du comportement du modèle GAMS. Les éventuels défauts identifiés sont également portés et documentés. L'intérêt d'une telle parité est de pouvoir attribuer correctement un écart à sa cause. En supprimant les divergences de transcription, on sait attribuer chaque divergence à une correction voulue.
La parité n'est pas l'aboutissement du modèle. C'est une première étape documentée avant amélioration du modèle et exploration des scénarios.
Ce souci de traçabilité a conduit à donner à chaque équation du GAMS un statut : portée, implicite, différée ou écartée. Quatorze équations Eq_*_SUPP sont
déclarées implicites car elles annulent des codes agrégats dont l’économie est nulle.
Eq_CS_GFA, qui impose la part minimale de canne des GFA, est portée mais désactivée : elle rend le problème infaisable sur les données 2017 réelles, comme le ferait le GAMS.

L’annexe B donne la table complète avec le motif de chaque statut

\section{Une architecture qui sépare le générique du territoire}
\label{sec:architecture}
les formules (2.1)–(2.4) ne mentionnent pas la Guadeloupe. C'est un des principes d'organisation du code, réparti en trois couches :
- un noyau générique, qui ne connaît que des parcelles, des cultures et des exploitations
- une étude de cas, qui fournit les données guadeloupéennes, les règles héritées du GAMS
et la restitution
- une couche en lecture seule, qui ouvre les dossiers de résultats déjà écrits.
Le respect de ce principe est garantie via un test de softcoding appliqué au noyau.
Le noyau définit des constructeurs d'objets. Chaque typologie d'objet a un constructeur propre. Par exemple, les contraintes qui partagent le formalisme de l'équation (2.4) partagent le même constructeur. Construire une contrainte, c'est d'abord récupérer ses caractéristiques dans un fichier YAML de configuration puis mobiliser le constructeur adéquat. Les caractéristiques deviennent les arguments de la fonction constructeur. Par exemple, un plafond nitrates, une enveloppe de dépense publique et un plancher d’emploi sont le même constructeur appelé avec trois arguments différents.
Au cours du chapitre 4, on verra que ce principe permet d'explorer 10 politiques, 12 forçages et 2 fronts de Pareto, c'est-à-dire un nombre considérable de combinaisons de contraintes, sans jamais modifier le noyau. Ce principe permet d'envisager l'utilisation du modèle à d'autres cas d'études et de simplifier l'exploration prospective.

\section{Rendre le problème traitable}
\label{sec:tractabilite}

Un résolution complète sur le territoire demande entre 30min et 55min. De plus, à taille constante et problème identique, 20 résolutions enregistrées s'étalent de 155s à 1007s.
--- POURQUOI DEUX FOURCHETTES DE TEMPS DIFFERENTES ? CE NE SONT PAS LES MEMES RESOLUTIONS ? ---

Cette variabilité vient de la recherche par séparation et évaluation (le coût de l'enveloppe logiciel étant de 15 s).

--- EXPLIQUE LE PRINCIPE DE CETTE RECHERCHE ET EXPLIQUE CE QU'EST L'ENVELOPPE LOGICIEL ---

Une limite de temps de calcul existe pour éviter un débordement trop important. Le problème est qu'un solve arrêté à cause de la limite de temps n'est pas fiable car il sous-estime probablement l'objectif final et propose une mosaïque qui diverge probablement de la mosaïque recherchée. Ce genre de solve peut être courant du fait de la variabilité des temps de calcul.
Pour remédier à ce problème, on propose le principe d'amorçage à chaud (warm-start). Le solveur reçoit l'allocation d'un run passé comme point de départ. Cela accélère grandement la recherche d'optimum mais ne change pas la nature du résultat. Par exemple, sur un même problème et pour un optimum identique, on passe de 720s à 209s de temps de calcul.
--- EXPLIQUE COMMENT PROUVER L'OPTIMALITE D'UN OPTIMUM ---
--- EXPLIQUE LES GRAINES ADMISSIBLES ET SI WARM START PEUT PRENDRE UN GRAINE DUN SCENARIO POUR LUTILISER POUR UN AUTRE SCENARIO ---

Pour finir, le solveur admet un écart d'optimalité pour la fonction objective ajustée du risque. Cette tolérance possède une valeur par défaut qui rend raisonnablement impossible la terminaison du calcul. On fixe donc cette valeur à 1%.
--- IMPOSSIBLE = TROP LONG ? ---


\section{Les garde-fous}
\label{sec:gardefous}
Il existe trois dispositifs différents qui contrôlent la robustesse du code :
- 613 tests unitaires, répartis en 58 fichiers dont le but est de vérifier que chaque contrainte fait ce qu'elle prétend. Ces tests sont rapides et exécutable sans données. Ils ne permettent pas de vérifier que le résultat global est juste.
- le golden snapshot, construit le vrai jeu de données et produits des indicateurs sur une allocation réelle. Puis, il sérialise 681 sommes de contrôle, c'est-à-dire qu'il compare
--- EXPLIQUE MIEUX LE GOLDEN SNAPSHOT C'EST UN CALCUL OU BIEN C'EST LE CHOIX D'UNE ALLOCATION AU HASARD ? ET LES CHECKSUM C'EST DES COMPARAISONS D'ORDRE DE GRANDEUR ? ET 681 C'EST LE NOMBRE DE GRANDEURS COMPAREES ? ---
Le golden snapshot se résout en 7s contre 30min à 55min pour un run classique.
- un script compare 3 runs de références déclarés à leurs métriques attendues. Le bruit de la recherche arborescente vaut 0,15 point de Pourcentage d’Écart Absolu (percentage absolute deviation) (PAD) (§ 3.6). Cela correspond à environ 5% de bruit sur l'objectif final. Le script à donc une tolérance de 5%.
--- POURQUOI APPELER CA "PAD"? CA DEVRAIT ETRE POURCENTAGE D'ERREUR EN FRANCAIS ET PERCENTAGE ERROR EN ANGLAIS NON ? ---

\begin{reflexion}
REFLEXIVITE
\end{reflexion}
