\chapter{Porter et rendre traçable}
\label{ch:porter}
% BUDGET : 6 pages (cf. memoire/PLAN.md, section 4).
% THESE DU CHAPITRE : la fidelite a l'ARTEFACT -- et non a une realite supposee -- est ce qui
% rend opposable tout desaccord ulterieur entre les deux modeles.
%
% NOTE : les blocs mathematiques sont CONSERVES. Ce ne sont pas de la prose a reecrire, ce
% sont les objets formels du memoire, et \eqref{eq:objectif}, \eqref{eq:mo} et
% \eqref{eq:indicateur} sont appeles depuis les chapitres 3 et 4.

\minisommaire{%
  \ms{sec:formulation}{le programme et sa fonction objectif}%
  \ms{sec:parite}{porter les défauts avec le reste}%
  \ms{sec:architecture}{ce qui est générique, ce qui est guadeloupéen}%
  \ms{sec:tractabilite}{rendre le problème résoluble}%
  \ms{sec:gardefous}{tests, sommes de contrôle, runs de référence}}

\section{Formulation du programme}
\label{sec:formulation}

Le programme est un Mixed Integer Linear Program – programme linéaire
en nombres entiers (MILP) purement binaire : il n’y a aucune variable continue

Soit $P$ l'ensemble des parcelles, $C$ l'ensemble des cultures, et $E \subseteq P \times C$
l'ensemble des couples \emph{éligibles} (§~\ref{sec:architecture}). 
L’ensemble $E$ rend le problème traitable computationnellement. Le produit $P \times C$ compte environ deux millions
de couples. Restreindre cet ensemble aux couples agronomiquement éligibles réduit le
compte à 308 847 (tableau 2.1). C’est une décision de modélisation qui intervient avant
l’optimisation : les couples écartés par le masque d’éligibilité ne pourront pas réapparaître
grâce à une contrainte. Le chapitre 3 montrera que le masque impose à lui seul un plancher
d’écart à l’observé. \\

La variable de décision est
\[
  Y_{p,c} \in \{0,1\}, \qquad (p,c) \in E,
\]
qui vaut 1 si la parcelle $p$ porte la culture $c$. \\

On utilise une fonction objective, c’est-à-dire une quantité que l’on cherche à optimiser. L'objectif à maximiser est la marge brute agrégée, corrigée du risque, appelée fonction de Markowitz :
\begin{equation}
  \max \; \sum_{(p,c) \in E} Y_{p,c} \; s_p \; m_c \; \bigl(1 - a_{f(p)} \, v_c \bigr),
  \label{eq:objectif}
\end{equation}
où $s_p$ est la surface de la parcelle (ha), $m_c$ la marge brute unitaire (\eur/ha), $f(p)$
l'exploitation de la parcelle, $a_f$ son coefficient d'aversion au risque et $v_c$ un
coefficient de perte propre à la culture. Une exploitation neutre au risque ($a_f = 0$) maximise la marge brute. 
Une exploitation prudente sur une culture volatile voit cette même marge fortement décotée. \\

À chaque parcelle, au plus une culture :
\begin{equation}
  \sum_{c \,:\, (p,c) \in E} Y_{p,c} \;\le\; 1, \qquad \forall p \in P.
  \label{eq:unicite}
\end{equation}
\\

Par exploitation, un plafond de main d'\oe uvre,
\begin{equation}
  \sum_{p \in P_f} \sum_{c} Y_{p,c} \, s_p \, h_c \;\le\; \sigma \, H_f,
  \label{eq:mo}
\end{equation}
où $h_c$ est le besoin en travail (h/ha), $H_f$ la main d'\oe uvre observée et $\sigma$ un
facteur de desserrement. \\

Enfin, des bornes portant sur un \emph{indicateur} quelconque,
\begin{equation}
  \sum_{(p,c) \in E} Y_{p,c} \, s_p \, w_p \, r_c \;\lesseqgtr\; T,
  \label{eq:indicateur}
\end{equation}
où $r_c$ est un taux par hectare (kg d'azote, \gls{ift}, heures, euros de subvention) et $w_p$
un poids de parcelle.\\


Deux précisions concernant la fonction objective :
\begin{itemize}
  \item La pénalité est linéaire en surface, et aucune covariance entre cultures n'intervient. Le
    programme appartient donc à la classe \gls{milp} plutôt que quadratique. Il est donc
    résoluble par \gls{highs}.
  \item Le revenu net que produit le rapport de run n'est pas la quantité maximisée. Il intègre
    également le coût de la main d'\oe uvre, alors qu'elle n'est \textbf{pas} monétisée dans les
    coûts variables hérités du \gls{gams} : elle n'apparaît que comme la ressource plafonnée
    de~\eqref{eq:mo}.
\end{itemize}

L'annexe~\ref{ann:formulation} donne l'inventaire complet des ensembles, paramètres et
contraintes, avec pour chacun le nom qu'il porte dans le \gls{gams} d'origine.


\section{Protocole}
\label{sec:parite}

Le protocole du portage prévoit une reproduction fidèle du comportement du modèle \gls{gams}.
Les éventuels défauts identifiés sont également portés et documentés. L'intérêt d'une telle
parité est de pouvoir attribuer correctement un écart à sa cause. En supprimant les divergences
de transcription, on sait attribuer chaque divergence à une correction voulue.

La parité n'est pas l'aboutissement du modèle. C'est une première étape documentée avant
amélioration du modèle et exploration des scénarios.

Ce souci de traçabilité a conduit à donner à chaque équation du \gls{gams} un statut : portée,
implicite, différée ou écartée. Quatorze équations \texttt{Eq\_*\_SUPP} sont déclarées implicites
car elles annulent des codes agrégats dont l'économie est nulle. \texttt{Eq\_CS\_GFA}, qui impose
la part minimale de canne des \gls{gfa}, est portée mais désactivée : elle rend le problème
infaisable sur les données 2017 réelles, comme le ferait le \gls{gams}.

L'annexe~\ref{ann:portage} donne la table complète avec le motif de chaque statut.

\section{Une architecture qui sépare le générique du territoire}
\label{sec:architecture}

Les formules \eqref{eq:objectif}--\eqref{eq:indicateur} ne mentionnent pas la Guadeloupe. C'est
un des principes d'organisation du code, réparti en trois couches :
\begin{itemize}
  \item un noyau générique, qui ne connaît que des parcelles, des cultures et des exploitations ;
  \item une étude de cas, qui fournit les données guadeloupéennes, les règles héritées du
    \gls{gams} et la restitution ;
  \item une couche en lecture seule, qui ouvre les dossiers de résultats déjà écrits.
\end{itemize}
Le respect de ce principe est garanti via un test de \emph{soft coding} appliqué au noyau.

Le noyau définit des constructeurs d'objets. Chaque typologie d'objet a un constructeur propre.
Par exemple, les contraintes qui partagent le formalisme de l'équation~\eqref{eq:indicateur}
partagent le même constructeur. Construire une contrainte, c'est d'abord récupérer ses
caractéristiques dans un fichier \gls{yaml} de configuration puis mobiliser le constructeur
adéquat. Les caractéristiques deviennent les arguments de la fonction constructeur. Par exemple,
un plafond nitrates, une enveloppe de dépense publique et un plancher d'emploi sont le même
constructeur appelé avec trois arguments différents.

Au cours du chapitre~\ref{ch:explorer}, on verra que ce principe permet d'explorer
\num{\mesPolitiques} politiques, \num{\mesForcages} forçages et \num{\mesBalayages} fronts de
Pareto, c'est-à-dire un nombre considérable de combinaisons de contraintes, sans jamais modifier
le noyau. Ce principe permet d'envisager l'utilisation du modèle à d'autres cas d'études et de
simplifier l'exploration prospective.

\section{Rendre le problème traitable}
\label{sec:tractabilite}

Une résolution complète sur le territoire demande entre \SI{30}{\minute} et \SI{55}{\minute}.
C'est un ordre de grandeur, toutes configurations confondues, pour de la calibration ou de la
prospective, à froid ou à chaud. C'est l'ordre de grandeur d'organisation qu'il faut prévoir
avant de lancer un run.

De plus, à taille constante (\num{\mesBinaires} variables) et problème identique, \num{20}
résolutions enregistrées se sont étalées de \SI{\mesSolveMin}{\second} à
\SI{\mesSolveMax}{\second}. Cette variabilité vient de la recherche par séparation et évaluation
(le coût de l'enveloppe logicielle étant de \SI{15}{\second}).

--- Pourquoi on ne retrouve pas les 30min-55min classiques ? ---

% CORRIGE : « variables entières entre 0 et 1 » -> « continues ». Un entier entre 0 et 1 vaut 0
% ou 1 : il n'y aurait alors aucune relaxation.
La recherche par séparation et évaluation (\emph{branch and bound}) permet de ne pas explorer
les $2^{\num{\mesBinaires}}$ combinaisons possibles. Elle permet plutôt l'exploration organisée
en arbre. Chaque niveau correspond au resserrement du problème d'une variable. On abandonne une
branche dès qu'on peut prouver, grâce à sa relaxation linéaire, c'est-à-dire sa résolution comme
problème à variables continues entre \num{0} et \num{1}, qu'elle ne battra jamais la meilleure
solution déjà trouvée. Si le solveur trouve rapidement une première solution, celle-ci devient
automatiquement la meilleure déjà trouvée, et il peut rapidement écarter des branches et gagner
du temps d'exploration.

Le solveur s'arrête dès que l'écart relatif entre l'\emph{incumbent}, c'est-à-dire la meilleure
solution déjà trouvée, et la meilleure valeur de relaxation parmi les n\oe uds encore ouverts
est suffisamment proche de \SI{0}{\percent}.

Le facteur $(1 - a_{f} v_{c})$ de l'équation~\eqref{eq:objectif} peut devenir négatif pour une
exploitation prudente sur une culture très variable. Cela peut rendre la relaxation linéaire
très fractionnaire et donc rallonger le temps nécessaire à la recherche d'optimum. On fixe donc
cette tolérance à \SI{\mesGapMip}{\percent} au lieu de la valeur par défaut de \gls{highs}, qui
est de \SI{0.01}{\percent}. On passe d'un solve de \SI{37}{\hour} qui n'avait pas terminé à des
solves d'\SI{1}{\hour} qui trouvent un optimum.

Une limite de temps de calcul existe pour éviter un débordement trop important. Le problème est
qu'un solve arrêté à cause de la limite de temps n'est pas fiable car il sous-estime probablement
l'objectif final et propose une mosaïque qui diverge probablement de la mosaïque recherchée. Ce
genre de solve peut être courant du fait de la variabilité des temps de calcul.

Pour remédier à ce problème, on propose le principe d'amorçage à chaud (\emph{warm start}). Le
solveur reçoit l'allocation d'un run passé comme point de départ. Cela accélère grandement la
recherche d'optimum mais ne change pas la nature du résultat. Par exemple, sur un même problème
et pour un optimum identique, on passe de \SI{\mesWarmAvant}{\second} à
\SI{\mesWarmApres}{\second} de temps de calcul.

La Table 2.1 récapitule ces informations :

\begin{table}[h]
  \centering
  \caption{Taille du programme et coût de sa résolution. Les durées sont mesurées sur le
  territoire complet ; l'étendue \emph{min--max} porte sur vingt résolutions du \emph{même}
  problème, à taille constante.}
  \label{tab:programme}
  \small
  \begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}Xr@{}}
    \toprule
    Couples parcelle $\times$ culture potentiels & $\sim$\num{2000000} \\
    Variables binaires déclarées (calibration retenue) & \num{\mesBinaires} \\
    Variables binaires déclarées (configuration prospective) & \num{\mesBinairesRef} \\
    \dots{} si les \num{25} variantes symétriques sont rouvertes (§~\ref{sec:tractabilite})
      & \num{\mesBinairesKarus} \\
    \midrule
    Résolution la plus rapide des vingt enregistrées & \SI{\mesSolveMin}{\second} \\
    Résolution la plus lente des vingt enregistrées & \SI{\mesSolveMax}{\second} \\
    Coefficient de variation des durées & \SI{\mesSolveCV}{\percent} \\
    Même problème, amorçage à froid puis à chaud
      & \SI{\mesWarmAvant}{\second} $\rightarrow$ \SI{\mesWarmApres}{\second} \\
    Écart d'optimalité relatif imposé au solveur & \SI{\mesGapMip}{\percent} \\
    \bottomrule
  \end{tabularx}
\end{table}

\section{Les garde-fous}
\label{sec:gardefous}

Il existe trois dispositifs différents qui contrôlent la robustesse du code :
\begin{itemize}
  \item \num{\mesTests} tests unitaires, répartis en \num{\mesTestsFichiers} fichiers dont le but
    est de vérifier que chaque contrainte fait ce qu'elle prétend. Ces tests sont rapides et
    exécutables sans données. Ils ne permettent pas de vérifier que le résultat global est juste.
  \item le \emph{golden snapshot} fixe sur chaque parcelle l'allocation de la culture
    représentative qu'elle porte réellement en \num{2017}. Le but ici est de comparer des
    indicateurs entre la référence et l'allocation que le \emph{golden snapshot} fixe. Les deux
    côtés sont censés être rigoureusement identiques. On sérialise ainsi \num{\mesChecksums}
    sommes de contrôle : sommes de colonnes, tailles de tables, etc. Ce script permet
    d'identifier les dérives numériques silencieuses du pipeline ou des indicateurs après une
    retouche. Il ne renseigne pas sur la justesse du modèle. Le \emph{golden snapshot} se résout
    en \SI{7}{\second} contre \SI{30}{\minute} à \SI{55}{\minute} pour un run classique.
  \item un script compare \num{3} runs de références déclarés à leurs métriques attendues. Le
    bruit de la recherche arborescente vaut \num{\mesBruitPad} point de \gls{pad}
    (§~\ref{sec:plateau}). Cela correspond à environ \SI{5}{\percent} de bruit sur l'objectif
    final. Le script a donc une tolérance de \SI{5}{\percent}.
\end{itemize}

\begin{reflexion}
REFLEXIVITE
\end{reflexion}
